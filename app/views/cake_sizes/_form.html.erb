<div class='form'>
<%= form_for(@cake_size) do |f| %>
  <% if @cake_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cake_size.errors.count, "error") %> prohibited this cake_size from being saved:</h2>

      <ul>
      <% @cake_size.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="fields">
    <%= f.label :name %>
    <%= f.text_field :name %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>